package com.zerodev.clen.domain.model

enum class JunkCategory {
    OWN_CACHE,
    RESIDUAL_APK,
    LARGE_FILE,
    EMPTY_FOLDER,
    OLD_DOWNLOAD,
    DUPLICATE_FILE,
    DUPLICATE_PHOTO,
    OLD_SCREENSHOT,
    WHATSAPP_MEDIA,
}
